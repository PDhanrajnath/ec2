apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bc15-gc.fullname" . }}-test-connection"
  labels:
    {{- include "bc15-gc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bc15-gc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
