apiVersion: v1
kind: Service
metadata:
  name: {{ include "bc15-gc.fullname" . }}
  labels:
    {{- include "bc15-gc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bc15-gc.selectorLabels" . | nindent 4 }}
